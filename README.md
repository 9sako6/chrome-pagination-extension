# Keyboard Pagination

Webサイトのページネーションを左右の矢印キーで操作する、自分用のChrome拡張です。

サイトごとに異なるページネーション形式へ、必要に応じて対応していきます。

## 操作

対応ページで `←` を押すと前へ、`→` を押すと次へ移動します。入力中や修飾キーとの同時押しには反応しません。

## インストール

1. Chromeで `chrome://extensions` を開く
2. 「デベロッパー モード」を有効にする
3. 「パッケージ化されていない拡張機能を読み込む」で、このリポジトリを選ぶ
